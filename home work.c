#include<stdio.h>
int main()
{
    int a;
    int b;
    int c;
    int Area;
    scanf("%lf%lf%lf",&a,&b,&c);
    if(a+b>c && b+c>a && c+a>b && Area = (a + b + c)/2;)
    {
        printf("Area of triangle\n");
    }
    else
    {
        printf("No triangle is possible\n");
    }
    retur n 0;
